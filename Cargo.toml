[package]
name = "rust1"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.5"
tokio = { version = "1.0", features = ["full"] }
tower = { version = "0.4.13", features = ["util", "timeout", "load-shed"]}
uuid = { version = "1.0", features = ["serde", "v4"] }
